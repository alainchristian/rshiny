dept_config <- list(
  id          = "youth_survey",
  name        = "Youth Survey",
  description = "Rwanda youth employment, education, and demographic survey analysis",
  icon        = "clipboard-list",
  menu_items  = list(
    list(label = "Dashboard", tabName = "ys_dashboard", icon = "chart-bar")
  )
)
